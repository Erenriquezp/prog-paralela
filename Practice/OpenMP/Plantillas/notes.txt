Variante 2: Diferencia de Cuadrados Absoluta ($\sum |X[i]^2 - Y[i]^2|$) = std::abs(diff)
Matriz[fila][columna] --> arreglo[fila * N + columna]
int idx = (i + p) * n + (i + p); // Fórmula diagonal: filia[i] * N + columna[i]

clangd